Per usare il codice:

from terminal:

$ pip install loguru tkinter sys multiprocessing

$ python Interface.py

NON SI DEVE FARE ALCUNA COMPILAZIONE CON C++, ci pensa gia' l'interfaccia al momento.
Robe da fixare:
 - Usare un opportuno path di eigen (va specificato)
 - Convertire eventualmente ad eigen la generazione degli indici casuali e degli iperparametri per consistenza nel codice